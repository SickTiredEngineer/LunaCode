<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.luna_code.mapper.OnlineClassMapper">

    <select id="selectUrlByClassId" resultType="String" parameterType="int">
        SELECT url 
        FROM class 
        WHERE class_idx = #{value}
    </select>
    
    <select id="selectClassesByUserId" resultType="com.itwillbs.luna_code.vo.ClassVo" parameterType="int">
	    SELECT c.class_idx, c.title, c.video_url, e.is_completed, e.enroll_date
		FROM class c
		JOIN class_enrollment e ON c.class_idx = e.class_idx
		WHERE e.user_idx = #{value}
	</select>
    
</mapper>
